import { createTheme } from "@rneui/themed";

export const theme = createTheme({
  lightColors: {
    primary: "#2563EB",
    background: "#FFFFFF",
  },
  darkColors: {
    primary: "#3B82F6",
    background: "#0F172A",
  },
  mode: "light",
});
